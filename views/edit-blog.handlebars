<div class='row'>
  <div class='card'>
    <div class='card-header' id="blogTitle">{{post.title}}</div>
    <div class='card-body'>
      <textarea id='edit-blog-textarea' id='blogContent'>{{post.content}}</textarea>
    </div>
    <div class='card-footer text-muted'>
      <button id='edit-blog-save-button' data-id='{{post.id}}'>Save Blog</button>
    </div>
  </div>
</div>